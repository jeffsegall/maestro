program testure{
	var double speed = 1.5;
	
	do RobotControl.setRightShoulderRollRad(-1.35, speed, 0)

	while RobotControl.requiresMotionByName("RSR", 0){
		do nothing
	}

	do RobotControl.setRightShoulderYawRad(1.57, speed, 0)

        while RobotControl.requiresMotionByName("RSY", 0){
                do nothing
        }
	
	do RobotControl.setRightElbowRad(-1.57, speed, 0)

	while (RobotControl.requiresMotionByName("REP", 0)){
		do nothing
	}

	do RobotControl.setRightElbowRad(0, speed,0)

	while (RobotControl.requiresMotionByName("REP", 0)){
		do nothing
	}

	do RobotControl.setRightElbowRad(-1.57, speed,0)

        while (RobotControl.requiresMotionByName("REP", 0)){
                do nothing
        }

        do RobotControl.setRightElbowRad(0, speed,0)

        while (RobotControl.requiresMotionByName("REP", 0)){
                do nothing
        }

	do RobotControl.setRightElbowRad(-1.57, speed,0)

        while (RobotControl.requiresMotionByName("REP", 0)){
                do nothing
        }

        do RobotControl.setRightElbowRad(0, speed,0)

        while (RobotControl.requiresMotionByName("REP", 0)){
                do nothing
        }
	
	do RobotControl.setRightShoulderYawRad(-1.57, speed,0)

	while RobotControl.requiresMotionByName("RSY", 0){
		do nothing
	}

	do RobotControl.setRightElbowRad(-1.57, speed,0)

        while (RobotControl.requiresMotionByName("REP", 0)){
                do nothing
        }

        do RobotControl.setRightElbowRad(0, speed,0)

        while (RobotControl.requiresMotionByName("REP", 0)){
                do nothing
        }

	do RobotControl.setRightElbowRad(-1.57, speed,0)

        while (RobotControl.requiresMotionByName("REP", 0)){
                do nothing
        }

        do RobotControl.setRightElbowRad(0, speed,0)

        while (RobotControl.requiresMotionByName("REP", 0)){
                do nothing
        }

	do RobotControl.setRightElbowRad(-1.57, speed,0)

        while (RobotControl.requiresMotionByName("REP", 0)){
                do nothing
        }

        do RobotControl.setRightElbowRad(0, speed,0)

        while (RobotControl.requiresMotionByName("REP", 0)){
                do nothing
        }
	

	do RobotControl.setRightShoulderYawRad(0, speed,0)
	do RobotControl.setRightShoulderRollRad(0, speed,0)


}
