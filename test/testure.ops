program testure{
	
	do RobotControl.setRightShoulderRollRad(-1.35, 0)

	while RobotControl.requiresMotion(8, 1, 0){
		do nothing
	}

	do RobotControl.setRightShoulderYawRad(1.57,0)

        while RobotControl.requiresMotion(9, 0, 0){
                do nothing
        }
	
	do RobotControl.setRightElbowRad(-1.57,0)

	while (RobotControl.requiresMotion(9, 1, 0)){
		do nothing
	}

	do RobotControl.setRightElbowRad(0,0)

	while (RobotControl.requiresMotion(9, 1, 0)){
		do nothing
	}

	do RobotControl.setRightElbowRad(-1.57,0)

        while (RobotControl.requiresMotion(9, 1, 0)){
                do nothing
        }

        do RobotControl.setRightElbowRad(0,0)

        while (RobotControl.requiresMotion(9, 1, 0)){
                do nothing
        }

	do RobotControl.setRightElbowRad(-1.57,0)

        while (RobotControl.requiresMotion(9, 1, 0)){
                do nothing
        }

        do RobotControl.setRightElbowRad(0,0)

        while (RobotControl.requiresMotion(9, 1, 0)){
                do nothing
        }
	
	do RobotControl.setRightShoulderYawRad(-1.57,0)

	while RobotControl.requiresMotion(9, 0, 0){
		do nothing
	}

	do RobotControl.setRightElbowRad(-1.57,0)

        while (RobotControl.requiresMotion(9, 1, 0)){
                do nothing
        }

        do RobotControl.setRightElbowRad(0,0)

        while (RobotControl.requiresMotion(9, 1, 0)){
                do nothing
        }

	do RobotControl.setRightElbowRad(-1.57,0)

        while (RobotControl.requiresMotion(9, 1, 0)){
                do nothing
        }

        do RobotControl.setRightElbowRad(0,0)

        while (RobotControl.requiresMotion(9, 1, 0)){
                do nothing
        }

	do RobotControl.setRightElbowRad(-1.57,0)

        while (RobotControl.requiresMotion(9, 1, 0)){
                do nothing
        }

        do RobotControl.setRightElbowRad(0,0)

        while (RobotControl.requiresMotion(9, 1, 0)){
                do nothing
        }
	

	do RobotControl.setRightShoulderYawRad(0,0)
	do RobotControl.setRightShoulderRollRad(0,0)


}
